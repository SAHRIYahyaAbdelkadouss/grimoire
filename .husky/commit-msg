#!/usr/bin/env sh
node scripts/validate-commit.mjs "$1"
status=$?

# exit 2 = skip (merge/fixup/squash), exit 1 = invalid message
if [ $status -eq 2 ]; then exit 0; fi
if [ $status -ne 0 ]; then exit $status; fi

npm test
